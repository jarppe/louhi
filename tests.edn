#kaocha/v1
 #meta-merge
  [{:tests [{:id         :unit
             :test-paths ["src/test"]
             :skip-meta  [:slow]}
            {:id         :slow
             :test-paths ["src/test"]
             :focus-meta [:slow]}]}]